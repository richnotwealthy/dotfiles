mv ~/.tmux.conf ~/.tmux.conf.old
mv ~/.vimrc ~/.vimrc.old

printf "so $HOME/dotfiles/vimrc.vim" > ~/.vimrc
printf "source-file $HOME/dotfiles/tmux.conf" > ~/.tmux.conf

echo
echo '.vimrc and .tmux.conf files linked to $HOME/dotfiles'
